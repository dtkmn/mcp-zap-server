{{- if and .Values.zap.enabled .Values.networkPolicy.zap.enabled }}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "mcp-zap-server.fullname" . }}-zap
  labels:
    {{- include "mcp-zap-server.zap.labels" . | nindent 4 }}
spec:
  podSelector:
    matchLabels:
      {{- include "mcp-zap-server.zap.selectorLabels" . | nindent 6 }}
  policyTypes:
    - Ingress
    - Egress
  ingress:
    - from:
        - podSelector:
            matchLabels:
              {{- include "mcp-zap-server.mcp.selectorLabels" . | nindent 14 }}
        {{- with .Values.networkPolicy.zap.extraIngress }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      ports:
        - protocol: TCP
          port: {{ .Values.zap.service.targetPort }}
  {{- if or .Values.networkPolicy.zap.egress.allowDns .Values.networkPolicy.zap.egress.extraEgress }}
  egress:
    {{- if .Values.networkPolicy.zap.egress.allowDns }}
    - to:
        - namespaceSelector:
            {{- toYaml .Values.networkPolicy.zap.egress.dns.namespaceSelector | nindent 12 }}
          podSelector:
            {{- toYaml .Values.networkPolicy.zap.egress.dns.podSelector | nindent 12 }}
      ports:
        - protocol: UDP
          port: 53
        - protocol: TCP
          port: 53
    {{- end }}
    {{- with .Values.networkPolicy.zap.egress.extraEgress }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
  {{- else }}
  egress: []
  {{- end }}
{{- end }}
